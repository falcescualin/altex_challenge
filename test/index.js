import { getCategories, getItemsByCategory } from "../constants/api";
import UserModel from "../models/User";
import ProductModel from "../models/Product";
import CategoryModel from "../models/Category";

// Tin sa mentionez ca nu am scris foarte multe teste la viata mea :)

describe("User", () => {
    const email = "falcescu.alin@TEST.com",
        password = "test123";

    it("Register", (done) => {});

    it("Login", (done) => {
        done();
    });
});

describe("Category", () => {
    it("Register", (done) => {
        done();
    });
});

describe("Product", () => {
    it("Register", (done) => {
        done();
    });
});

describe("Altex API", () => {});
